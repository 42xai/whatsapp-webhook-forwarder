version: "3"
services:
  whatsapp-webhook-forwarder:
    container_name: whatsapp-webhook-forwarder
    image: ghcr.io/42xai/whatsapp-webhook-forwarder:latest
    command: npm run start
    restart: unless-stopped
    networks:
      - management_common
    ports:
      - 3700:3000

networks:
  management_common:
    external: true
